import React from 'react';
import { View, StyleSheet, Text } from 'react-native';
const ListaContato = (props) => {
 return (
 <View>
 {ListaContato}
 </View>
 )
};
const estilos = StyleSheet.create({
});
export default ListaContato;